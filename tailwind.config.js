// Not used. Tailwind CSS v4 is configured in CSS (resources/css/app.css via
// @import "tailwindcss" + @source) and auto-detects content. JS config files
// are NOT loaded unless explicitly referenced with @config. Safe to delete.
module.exports = {};
